---
title: "Référence API assurance"
description: "Lire les endpoints avec leurs paramètres, champs requis, réponses et exemples multi-langages."
icon: "braces"
---

La section **Référence API** est la documentation détaillée pour les intégrateurs ERP assurance. Chaque endpoint y expose :

- les headers, paramètres d'URL et query params ;
- les champs requis et optionnels du body ;
- la description métier de chaque champ ;
- les exemples de requête et de réponse ;
- les erreurs possibles ;
- les snippets `curl`, TypeScript, JavaScript, PHP, Java et C# générés par Mintlify.

## Lecture des champs

| Indication | Sens |
| --- | --- |
| `Required` | Champ obligatoire. L'appel est rejeté si le champ est absent ou vide. |
| `Optional` | Champ optionnel. Il peut être omis, sauf règle métier contextualisée. |
| `nullable` | Champ qui peut exister avec la valeur `null`. |
| `enum` | Valeurs acceptées exclusivement. Toute autre valeur retourne une erreur de validation. |
| `format: uuid` | Identifiant public stable au format UUID. |
| `format: date` | Date civile au format `YYYY-MM-DD`. |
| `format: date-time` | Date heure ISO 8601 avec fuseau, par exemple `2026-05-26T12:30:00Z`. |

## Headers communs

| Header | Requis | Description |
| --- | --- | --- |
| `X-Api-Key` | Oui | Clé API assurance. Elle détermine le tenant assureur, l'environnement et les scopes. |
| `X-Request-Id` | Non | Identifiant de corrélation fourni par l'ERP pour suivre une opération dans les logs et audits. |
| `Idempotency-Key` | Selon endpoint | Clé stable pour éviter les doublons sur les créations rejouables après timeout. |

Envoyez uniquement les champs documentés pour chaque endpoint. Les informations de périmètre (`tenant_id`, `insurer_uid`) sont déterminées par la clé API et ne doivent jamais être fournies dans le body ou les query params.

## Environnements

| Environnement | URL de base |
| --- | --- |
| Sandbox | `https://insurer-api-sandbox.elenami.com` |
| Production | `https://insurer-api.elenami.com` |

Le sélecteur d'URL en haut de chaque page d'endpoint permet de basculer entre sandbox et production. Le playground interactif vous permet de tester les endpoints directement depuis la documentation en saisissant votre clé API.

## Catégories d'endpoints

| Catégorie | Description |
| --- | --- |
| **Authentification** | Vérifier une clé API et récupérer le contexte assureur. |
| **Membres** | Créer, consulter et mettre à jour les membres (titulaires de police). |
| **Souscriptions** | Gérer les souscriptions et leur cycle de vie (activation, renouvellement). |
| **Bénéficiaires** | Gérer les bénéficiaires rattachés à une souscription. |
| **Éligibilité** | Vérifier l'éligibilité d'un membre pour une couverture donnée. |
| **Claims** | Consulter, statuer et gérer le cycle de vie des claims. |
| **Pré-autorisations** | Listez, consulter et décider les demandes de pré-autorisation. |
| **Détection de fraude** | Déclencher et consulter les évaluations de fraude sur les claims. |
| **Plans & couvertures** | Gérer les plans, types de garanties et calculs de remboursement. |
| **Référentiels cliniques** | Consulter les actes médicaux, médicaments et pathologies du référentiel Azurance. |
| **Réseau prestataires** | Gérer le réseau de prestataires et les demandes d'agrément. |
| **Facturation** | Gérer les factures payables, paiements et allocations. |
| **Webhooks** | Configurer et gérer les souscriptions webhook. |
| **Analytics** | Consulter les indicateurs portfolio, claims, loss ratio et performance prestataires. |
| **Notifications** | Gérer les notifications et préférences de l'assureur. |

## Réponses et erreurs

Les réponses `2xx` décrivent le résultat métier attendu. Les réponses `4xx` indiquent une erreur côté ERP, clé API, scopes, validation métier ou idempotence. Les réponses `5xx` indiquent une indisponibilité temporaire du service.

Les erreurs stables sont documentées dans [Erreurs API](/commun/erreurs). Pour les opérations critiques, conservez toujours `X-Request-Id` côté ERP : c'est la valeur à transmettre au support pour retrouver l'audit corrélé.
